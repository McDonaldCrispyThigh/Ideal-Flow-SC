\documentclass[11pt]{article}

\usepackage[margin=1in, top=1.15in, bottom=1.15in]{geometry}

\usepackage{amsmath, amssymb, amsthm}

\usepackage{graphicx}
\usepackage{subcaption}
\usepackage{float}

\usepackage{microtype}
\usepackage{lmodern}

\usepackage{xurl}
\usepackage[colorlinks=true, linkcolor=blue, citecolor=blue, urlcolor=blue,
            breaklinks=true]{hyperref}

\usepackage{natbib}
\bibliographystyle{plainnat}

\usepackage{booktabs}
\usepackage{enumitem}
\usepackage{xcolor}

\newcommand{\HH}{\mathbb{H}}
\newcommand{\CC}{\mathbb{C}}
\newcommand{\RR}{\mathbb{R}}
\newcommand{\dif}{\,\mathrm{d}}
\newcommand{\conj}[1]{\overline{#1}}
\newcommand{\abs}[1]{\left\lvert #1 \right\rvert}
\newcommand{\Imag}{\mathrm{Im}}
\newcommand{\Real}{\mathrm{Re}}

\newtheorem{theorem}{Theorem}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{remark}{Remark}

\begin{document}

\begin{titlepage}
  \centering
  \vspace*{3.5cm}
  {\LARGE\bfseries Ideal Fluid Flow in a Polygonal Domain\\[0.6em]
   via the Schwarz--Christoffel Transformation\par}
  \vspace{2.5cm}
  {\large
    Congyuan Zheng\\
    \small Dept.\ of Applied Mathematics \& Dept.\ of Geography,
    University of Colorado Boulder\\[1em]
    \large Sophia Arany\\
    \small Dept.\ of Applied Mathematics,
    University of Colorado Boulder\\[1em]
    \large Alexander Ingalls\\
    \small Dept.\ of Aerospace Engineering,
    University of Colorado Boulder\par}
  \vspace{2.5cm}
  {\normalsize APPM 4360, Complex Variables and Applications,
   Spring 2026\\[0.4em]
   April 27, 2026\par}
\end{titlepage}

\begin{abstract}
This paper applies the Schwarz--Christoffel (SC) conformal mapping to simulate
steady, irrotational, incompressible ideal fluid flow inside a 12-vertex
simplification of the Boulder County, Colorado boundary polygon. The SC
transformation maps the upper half-plane $\HH$ conformally onto the polygonal
domain $\Omega$, converting analytically tractable complex potentials in $\HH$
into streamline and equipotential patterns in the physical domain. Two flow
models are derived and implemented: a uniform ideal flow $W = U\zeta$
providing the baseline conformal grid, and a doubly-connected flow in which a
downtown obstacle is modelled via the Milne-Thomson circle theorem. The SC
parameter problem is solved by softmax pre-vertex parameterisation combined
with Levenberg--Marquardt least squares on side-length ratios computed by
Gauss--Legendre quadrature. All results are produced through the forward SC
map, yielding streamlines that satisfy the no-penetration boundary condition
exactly by construction. A closed-form verification on the rectangle-to-half-plane
map confirms the implementation.
\end{abstract}

\newpage

\section{Introduction}

Computing fluid flow in an arbitrary domain is a hard problem: Laplace's
equation $\nabla^2 \phi = 0$ admits no elementary closed-form solution in an
irregular geometry. The classical approach uses conformal mapping. A conformal
map $z = f(\zeta)$ from a canonical domain (the unit disk or the upper
half-plane) to the physical region $\Omega$ transforms the Laplace equation
from one domain to the other while preserving harmonicity, so that any
analytic function $W(\zeta)$ satisfying the required boundary conditions in
the canonical domain produces a valid solution in $\Omega$ via
$\tilde{W}(z) = W(f^{-1}(z))$.

The Schwarz--Christoffel (SC) transformation, developed independently by
H.~A.~Schwarz and E.~B.~Christoffel in the 1860s, gives an explicit integral
formula for the conformal map from the upper half-plane $\HH$ to any simply
connected polygon. For analytic polygons the parameter problem (choosing
pre-images of the vertices on the real axis) can sometimes be solved in
closed form; for general $n$-gons it must be treated numerically. The modern
numerical theory was systematised by Driscoll and Trefethen~\citep{DT09}; the
classical theory is in Nehari~\citep{Nehari52} and Ahlfors~\citep{Ahlfors79}.

We apply the SC transformation to a 12-vertex polygon derived from the
Boulder County, Colorado boundary, chosen for its mix of convex and reflex
vertices as a non-trivial test of the numerical pipeline. Two flow regimes
are considered: (i)~uniform ideal flow, providing the baseline conformal grid,
and (ii)~doubly-connected flow with an interior obstacle modelled via the
Milne-Thomson circle theorem. The polygon's geometric complexity exercises
the full pipeline; any physical interpretation as "airflow over a city" is
illustrative. The mathematical framework is drawn from
\citet{AF03,DT09,Nehari52}; the fluid-dynamics background from \citet{BO99}
and \citet{MT68}.

A closed-form verification against the rectangle-to-half-plane map (an
elliptic-integral identity) is included to certify the numerical pipeline
independently of the Boulder application.

\paragraph{Organisation.}
Section~\ref{sec:theory} develops the SC mapping theory and complex-potential
framework, with step-by-step derivations of every key result.
Section~\ref{sec:numerical} describes the numerical algorithms and verifies
the pipeline against the closed-form rectangle map. Section~\ref{sec:apply}
applies the method to the Boulder polygon and presents results for both flow
models. Section~\ref{sec:conclusions} summarises the conclusions. Technical
details on the Levenberg--Marquardt solver are in Appendix~\ref{app:lm};
code availability in Appendix~\ref{app:code}.

\section{Mathematical Framework}
\label{sec:theory}

\subsection{Ideal Fluid Flow and the Complex Potential}
\label{sec:ideal-flow}

A steady two-dimensional flow is \emph{ideal} when the velocity field
$\mathbf{u} = (u, v)$ is simultaneously irrotational
($\partial_x v - \partial_y u = 0$) and incompressible
($\partial_x u + \partial_y v = 0$). Irrotationality in a simply connected
region forces $\mathbf{u}$ to be a gradient field $\mathbf{u} = \nabla \phi$
(velocity potential), and incompressibility then gives $\nabla^2 \phi = 0$.
Incompressibility independently forces $\mathbf{u}$ to be the skew-gradient of
a stream function $\psi$ via $u = \partial_y \psi$, $v = -\partial_x \psi$, and
irrotationality then gives $\nabla^2 \psi = 0$. Comparing the two
representations produces the Cauchy--Riemann equations
\begin{equation}
  \partial_x \phi = \partial_y \psi,
  \qquad
  \partial_y \phi = -\partial_x \psi,
  \label{eq:CR}
\end{equation}
so the \emph{complex potential}
\begin{equation}
  W(z) = \phi(x, y) + i\,\psi(x, y), \qquad z = x + iy,
  \label{eq:W}
\end{equation}
is analytic on the flow domain.

\paragraph{Velocity from $W'(z)$.}
For any analytic function the complex derivative can be evaluated along the
real direction: $W'(z) = \partial_x W = \partial_x \phi + i\, \partial_x \psi$.
Using $\partial_x \phi = u$ and the Cauchy--Riemann equation
$\partial_x \psi = -\partial_y \phi = -v$, we obtain
\begin{equation}
  W'(z) = u - i v = \conj{u + iv},
  \qquad
  \mathbf{u} = \conj{W'(z)}.
  \label{eq:velocity}
\end{equation}

\paragraph{Physical interpretation.}
Level curves of $\phi$ are \emph{equipotentials}; level curves of $\psi$ are
\emph{streamlines}. The Cauchy--Riemann equations imply
$\nabla \phi \cdot \nabla \psi = 0$, so streamlines and equipotentials meet
orthogonally wherever $W'(z) \neq 0$. The no-penetration condition on a solid
boundary $\partial \Omega$ reads $\mathbf{u} \cdot \hat{\mathbf{n}} = 0$,
equivalent to $\psi = \mathrm{const}$ along $\partial \Omega$.

\subsection{Conformal Invariance of Harmonicity}
\label{sec:conformal}

\begin{lemma}
\label{lem:conformal}
Let $f : \HH \to \Omega$ be a bijective analytic map with $f'(\zeta) \neq 0$.
If $W(\zeta)$ is analytic on $\HH$, then $\tilde{W}(z) = W(f^{-1}(z))$ is
analytic on $\Omega$; in particular, both $\tilde{\phi} = \Real \tilde{W}$
and $\tilde{\psi} = \Imag \tilde{W}$ satisfy Laplace's equation on $\Omega$.
\end{lemma}

This is the structural fact that makes conformal mapping work for ideal flow.
Any solution of Laplace's equation in $\HH$ pulls back to a solution in
$\Omega$, and the no-penetration condition $\psi = 0$ on $\partial \HH$
(easy to arrange by the method of images) becomes $\psi = 0$ on
$\partial \Omega$ automatically.

\subsection{The Riemann Mapping Theorem}

\begin{theorem}[Riemann, 1851]
\label{thm:riemann}
Let $\Omega \subsetneq \CC$ be a simply connected domain. There exists a
bijective conformal map $f : \HH \to \Omega$, unique up to post-composition
with a real M\"obius transformation of $\HH$.
\end{theorem}

The Boulder polygon is simply connected, so $f$ exists. The three real
parameters of the M\"obius freedom correspond to fixing three real pre-images
on $\partial \HH = \RR \cup \{\infty\}$.

\subsection{The Schwarz--Christoffel Formula}
\label{sec:sc-formula}

For a polygon $\Omega$ with vertices $w_1, \dots, w_n$ (counter-clockwise) and
interior angles $\alpha_k \pi$, $\alpha_k \in (0, 2)$, the SC map takes the
form~\citep[Ch.~5]{Nehari52}\citep[Ch.~5]{DT09}
\begin{equation}
  f(\zeta) = A + C \int_{\zeta_0}^{\zeta}
    \prod_{k=1}^{n} (t - \zeta_k)^{\alpha_k - 1} \dif t,
  \label{eq:SC}
\end{equation}
where $\zeta_k \in \RR$ are the \emph{pre-vertices}, $A \in \CC$ is a
translation, and $C \in \CC$ encodes scale and rotation. We derive the four
structural properties that force~\eqref{eq:SC}.

\paragraph{Step 1: Polygon angle-sum identity.}
For a simple CCW polygon the exterior turning angle at vertex $k$ is
$\pi - \alpha_k \pi = \pi(1 - \alpha_k)$. The total exterior turning over a
full CCW traversal equals $2\pi$, hence
\begin{equation}
  \sum_{k=1}^{n} \pi(1 - \alpha_k) = 2\pi
  \quad\Longleftrightarrow\quad
  \sum_{k=1}^{n} \alpha_k = n - 2.
  \label{eq:anglesum}
\end{equation}

\paragraph{Step 2: Direction of $f'(\zeta)$ on the real axis.}
Take $\zeta = t$ real with $t \in (\zeta_k, \zeta_{k+1})$. For each factor
$(t - \zeta_j)^{\alpha_j - 1}$:
\begin{itemize}[leftmargin=2em, itemsep=0.25em]
  \item if $j \leq k$ (so $t > \zeta_j$), the base is positive real and
        $\arg[(t - \zeta_j)^{\alpha_j - 1}] = 0$;
  \item if $j > k$ (so $t < \zeta_j$), the base is negative real and
        $\arg[(t - \zeta_j)^{\alpha_j - 1}] = (\alpha_j - 1) \pi$
        (principal branch, $\arg(-1) = \pi$).
\end{itemize}
Therefore
\begin{equation}
  \arg f'(t) = \arg C + \pi \sum_{j = k+1}^{n} (\alpha_j - 1).
  \label{eq:argfprime}
\end{equation}
This is constant on the open interval $(\zeta_k, \zeta_{k+1})$, so the image
$f\bigl((\zeta_k, \zeta_{k+1})\bigr)$ is a straight line segment, which is the
$k$-th polygon edge.

As $t$ increases past $\zeta_k$, the contribution of the $k$-th factor to
$\arg f'$ decreases by $(\alpha_k - 1) \pi$, i.e., the tangent direction turns
CCW by $(1 - \alpha_k) \pi$. This is exactly the exterior turning angle at
$w_k$, consistent with an interior angle of $\alpha_k \pi$.

\paragraph{Step 3: Vertex angle reproduction.}
The direction jump at vertex $w_k$ is the exterior turning $(1 - \alpha_k)\pi$,
so the interior angle is $\pi - (1 - \alpha_k)\pi = \alpha_k \pi$, reproducing
the prescribed geometry.

\paragraph{Step 4: Convergence at infinity.}
If $\zeta_n = \infty$ (one vertex is sent to infinity) the factor
$(t - \zeta_n)^{\alpha_n - 1}$ is dropped. Near $t \to \infty$ the remaining
integrand behaves as $t^{\sum_{j=1}^{n-1}(\alpha_j - 1)}$. Using~\eqref{eq:anglesum},
\begin{equation}
  \sum_{j=1}^{n-1} (\alpha_j - 1) = \biggl(\sum_{j=1}^{n} \alpha_j\biggr) - \alpha_n - (n - 1)
  = (n - 2) - \alpha_n - (n - 1) = -\alpha_n - 1,
\end{equation}
so the integrand decays as $t^{-\alpha_n - 1}$ and the integral converges at
infinity provided $\alpha_n > 0$.

\paragraph{Interior-angle formula in practice.}
Given vertices $w_0, \dots, w_{n-1}$ (CCW), the signed exterior turning at
$w_k$ is $\arg\!\bigl((w_{k+1} - w_k) / (w_k - w_{k-1})\bigr)$, so the interior
angle in units of $\pi$ is
\begin{equation}
  \alpha_k = 1 - \frac{1}{\pi}\,
    \arg\!\left( \frac{w_{k+1} - w_k}{w_k - w_{k-1}} \right),
  \label{eq:alpha}
\end{equation}
with indices modulo $n$.

\subsection{M\"obius Normalisation and the Parameter Problem}

The real M\"obius group $\mathrm{PSL}(2, \RR)$, acting on $\HH$ via
$\zeta \mapsto (a\zeta + b)/(c\zeta + d)$, has three real parameters. Three of
the $n$ pre-vertices can therefore be fixed at prescribed values without loss
of generality. We use
\begin{equation}
  \zeta_0 = -1, \qquad \zeta_1 = 0, \qquad \zeta_{n-1} = 1,
  \label{eq:normalisation}
\end{equation}
leaving $n - 3$ free parameters $\zeta_2 < \cdots < \zeta_{n-2}$ in $(0, 1)$.
For Boulder County, $n = 12$ yields $n - 3 = 9$ free pre-vertices. These are
determined by matching side-length ratios; see Section~\ref{sec:param}.

\subsection{Method of Images}
\label{sec:images}

Any analytic $W(\zeta)$ with $\Imag W = 0$ on $\RR$ automatically satisfies
$\psi = 0$ on $\partial \Omega$ after pull-back via $f$. To add a singularity
at $s = a + ib \in \HH$ (with $b > 0$) while preserving $\Imag W = 0$ on the
real axis, we pair it with a mirror-image singularity at $\bar{s} = a - ib$.
For a source of strength $Q > 0$ the image-pair potential is
\begin{equation}
  W_{\mathrm{source}}(\zeta)
  = \frac{Q}{2\pi} \bigl[ \log(\zeta - s) + \log(\zeta - \bar{s}) \bigr].
  \label{eq:source-pair}
\end{equation}

\paragraph{Algebraic verification.}
Take $\zeta = x \in \RR$:
\begin{align}
  \log(x - s) + \log(x - \bar{s})
  &= \log\!\bigl[(x - a)^2 + b^2\bigr]
     + i\,\bigl[\arg(x - a - ib) + \arg(x - a + ib)\bigr].
\end{align}
The numbers $x - a - ib$ and $x - a + ib$ are complex conjugates, so their
arguments are opposite in sign, making the bracketed imaginary part vanish.
Therefore
\begin{equation}
  \Imag\!\bigl[\log(x - s) + \log(x - \bar{s})\bigr] = 0
  \quad \text{for all } x \in \RR.
  \label{eq:imsum}
\end{equation}

\subsection{Milne-Thomson Circle Theorem}
\label{sec:MT}

\begin{theorem}[Milne-Thomson, 1940]
\label{thm:MT}
Let $W_0(\zeta)$ be analytic on $\CC \setminus D$ where
$D = \{\zeta : \abs{\zeta - \zeta_0} \leq a\}$, with no singularities on
$\partial D$. Then
\begin{equation}
  W^{\mathrm{ext}}(\zeta) = W_0(\zeta)
  + \conj{W_0\!\left(\zeta_0 + \frac{a^2}{\conj{\zeta - \zeta_0}}\right)}
  \label{eq:MT}
\end{equation}
is analytic on the exterior of $D$, has the same singularity structure there
as $W_0$, and satisfies $\Imag W^{\mathrm{ext}} = 0$ on $\partial D$.
\end{theorem}

\begin{proof}
\textit{Streamline on $\partial D$.}
Write $\zeta - \zeta_0 = a e^{i\theta}$ so that $\abs{\zeta - \zeta_0} = a$
and
\begin{equation}
  (\zeta - \zeta_0)\,\conj{(\zeta - \zeta_0)} = a^2
  \;\;\Longrightarrow\;\;
  \frac{a^2}{\conj{\zeta - \zeta_0}} = \zeta - \zeta_0,
  \label{eq:circle-id}
\end{equation}
hence on $\partial D$:
$\zeta_0 + a^2/\conj{\zeta - \zeta_0} = \zeta_0 + (\zeta - \zeta_0) = \zeta$.
Substituting into~\eqref{eq:MT} gives
\begin{equation}
  W^{\mathrm{ext}}(\zeta)\bigr|_{\partial D}
  = W_0(\zeta) + \conj{W_0(\zeta)}
  = 2\,\Real W_0(\zeta) \in \RR,
\end{equation}
so $\Imag W^{\mathrm{ext}} = 0$ on $\partial D$.

\textit{Analyticity outside $D$ via Schwarz reflection.}
Define $\iota(\zeta) = \zeta_0 + a^2 / \conj{\zeta - \zeta_0}$, which maps
$\CC \setminus \overline{D}$ bijectively onto $D \setminus \{\zeta_0\}$. Since
$\iota$ depends on $\conj{\zeta}$ rather than $\zeta$, the composition
$W_0 \circ \iota$ is a holomorphic function of $\conj{\zeta}$ on the exterior
of $D$, equivalently an anti-holomorphic function of $\zeta$. Taking the
complex conjugate of the entire expression, $\conj{W_0(\iota(\zeta))}$,
converts this into a holomorphic function of $\zeta$ on
$\CC \setminus \overline{D}$. This is the Schwarz reflection principle
specialised to a circle \citep[Ch.~4.6]{Ahlfors79}. Consequently
$W^{\mathrm{ext}} = W_0(\zeta) + \conj{W_0(\iota(\zeta))}$ is holomorphic in
$\zeta$ outside $D$, and its singularities there are exactly those of $W_0$
(those of $W_0 \circ \iota$ all lie inside $D$, where they are excluded by
hypothesis from the problem domain).
\end{proof}

\paragraph{Upper-half-plane specialisation.}
Equation~\eqref{eq:MT} gives a potential with $\partial D$ as a streamline,
valid on the whole complex plane exterior to $D$. To additionally enforce
$\psi = 0$ on the real axis $\partial \HH$, the method of images from
Section~\ref{sec:images} requires adding a mirror term obtained by reflecting
the obstacle centre $\zeta_0$ across the real axis. The mirror image of the
dipole at $\zeta_0$ is a dipole of the same strength at $\conj{\zeta_0}$ in
the lower half-plane, contributing $U a^2 / (\zeta - \conj{\zeta_0})$. For
$W_0 = U\zeta$ the combined expression
\begin{equation}
  W_{\mathrm{urban}}(\zeta) = U \zeta
    + \frac{U a^2}{\zeta - \zeta_0}
    + \frac{U a^2}{\zeta - \conj{\zeta_0}}
  \label{eq:urban}
\end{equation}
is holomorphic in $\zeta$ (the variable in every denominator is $\zeta$
itself), and both conditions hold simultaneously: the first additional term
makes $\partial D$ a streamline; the second restores $\psi = 0$ on $\RR$.

Figure~\ref{fig:mt-schematic} summarises the construction pictorially: a
true dipole at $\zeta_0 \in \HH$, its reflection $\conj{\zeta_0}$ in the
lower half-plane, and the obstacle circle together with its mirror; a few
schematic streamlines bend around the obstacle while remaining horizontal
far away.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.75\textwidth]{figures/figE_milne_thomson_schematic.png}
  \caption{\textbf{Milne-Thomson image construction in $\mathbb{H}$.}
  Red: obstacle centre $\zeta_0$ and its circle $\abs{\zeta - \zeta_0} = a$.
  Blue: real-axis mirror $\conj{\zeta_0}$ and mirror circle (dashed) in the
  lower half-plane. The dipole direction is indicated by horizontal arrows
  at each centre. Schematic streamlines (green) show how the dipole sum
  simultaneously enforces $\psi = \mathrm{const}$ on
  $\abs{\zeta - \zeta_0} = a$ and $\psi = 0$ on $\partial\HH$.}
  \label{fig:mt-schematic}
\end{figure}

\paragraph{Approximation error.}
When the actual obstacle boundary differs from a circle, approximating it by
the minimum enclosing circle introduces a leading-order error that scales as
$O\bigl((a/\Imag\,\zeta_0)^2\bigr)$~\citep[Ch.~6]{MT68}. This is an
order-of-magnitude estimate rather than a rigorous bound. Direct verification
against an exact doubly-connected SC map is noted in
Section~\ref{sec:conclusions} as future work.

\subsection{SC Crowding and the Conformal Modulus}
\label{sec:crowding}

A practical obstruction to numerical SC is \emph{crowding}: for polygons with
strongly different edge-length scales, the pre-vertices cluster exponentially
on the real axis. The phenomenon is not an artefact of the solver; it reflects
a rigid complex-analytic invariant.

\paragraph{Conformal modulus of a rectangle.}
Consider the rectangle $R = [-K, K] \times [0, K']$ of width $2K$ and height
$K'$. Its \emph{conformal modulus} (aspect ratio) is
$m(R) = 2K/K'$~\citep[Ch.~2]{DT09}, and it is a conformal invariant: no
conformal map sends $R$ to a rectangle of different modulus. The SC
pre-vertices $\{-1/k, -1, 1, 1/k\}$ that map to the corners of $R$ are related
to $m(R)$ through the complete elliptic integrals
\begin{equation}
  K(k) = \int_0^1 \frac{\dif t}{\sqrt{(1 - t^2)(1 - k^2 t^2)}},
  \qquad
  K'(k) = K\!\bigl(\sqrt{1 - k^2}\bigr),
  \label{eq:elliptic}
\end{equation}
with $m(R) = 2K(k) / K'(k)$~\citep[{\S}19.2]{NIST10}. As $m(R) \to \infty$
the elliptic modulus $k \to 1$, and the standard asymptotic expansion of
$K(k)$~\citep[{\S}19.12]{NIST10} gives the inner pre-vertex spacing
\begin{equation}
  1/k - 1 \;\sim\; 8 e^{-\pi m(R) / 2}
  \qquad \text{as } m(R) \to \infty.
  \label{eq:crowd-asym}
\end{equation}

\paragraph{Consequence for general polygons.}
For a general polygon containing a long thin region of aspect ratio $L$, the
pre-vertex spacing across that region contracts exponentially in
$L$~\citep[Ch.~2]{DT09}. Figure~\ref{fig:crowding} shows both the theoretical
crowding curve and the pre-vertex spacings recovered by our SC solver on a
family of rectangles of varying aspect ratio. The spacing drops below
$10^{-4}$ at $L \approx 4$ and below $10^{-8}$ at $L \approx 7$; beyond
$L \gtrsim 10$ the spacing approaches double precision and the parameter
problem becomes ill-posed. This limits SC solvers to $n \lesssim 20$ vertices
with mild aspect ratios, and is the reason the 2752-vertex Boulder County boundary
must be simplified to a 12-vertex polygon (Section~\ref{sec:preprocess}).
The chosen simplification has all interior angles confined to
$[0.35\pi, 1.75\pi]$, keeping the conformal modulus of each sub-region within
the stable regime.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.78\textwidth]{figures/figB_sc_crowding.png}
  \caption{\textbf{SC crowding: pre-vertex spacing as a function of
  rectangle aspect ratio $L = 2K(k)/K'(k)$.} Solid blue: exact inner spacing
  $1 - s$ in the pipeline normalisation $\{-1, 0, s, 1\}$, where
  $s = 2k/(1 + k^2)$ by~\eqref{eq:s-of-k}. Dashed grey: classical
  asymptotic $8 e^{-\pi L/2}$ for $1/k - 1$ in the symmetric normalisation.
  Red circles: spacings recovered by our SC solver on rectangles of aspect
  ratio $L \in \{1.5, 2, 2.5, 3, 3.5, 4, 5, 6, 7\}$; they track the
  theoretical curve until the quadrature error of plain Gauss--Legendre at
  the branch-point endpoints ($\sim 10^{-3}$--$10^{-4}$) dominates. The
  dashed orange line at $10^{-8}$ marks the threshold below which the
  pre-vertex spacing falls within the LM solver's default tolerance and
  can no longer be reliably recovered.}
  \label{fig:crowding}
\end{figure}

\section{Numerical Implementation}
\label{sec:numerical}

\subsection{Polygon Preprocessing}
\label{sec:preprocess}

The raw Boulder County TIGER/Line boundary~\citep{TIGER25} has 2752 vertices.
Douglas--Peucker simplification with an adaptive binary-search tolerance
reduces it to 14 vertices; two vertices with excessively acute angles below
$0.35\pi$ are then removed iteratively by merging adjacent edges, yielding the
final $n = 12$ polygon. The polygon is normalised to $O(1)$ scale and centred
at the origin for numerical stability. The angle bounds $[0.35\pi, 1.75\pi]$
are chosen from the crowding discussion of Section~\ref{sec:crowding}.

\subsection{The SC Parameter Problem}
\label{sec:param}

The $n - 3 = 8$ free pre-vertices $\zeta_2, \dots, \zeta_{n-2} \in (0, 1)$
are chosen so that the image polygon $f(\RR)$ has the correct side-length
ratios. Define the $k$-th side length
\begin{equation}
  L_k = \abs{\, \int_{\zeta_k}^{\zeta_{k+1}}
    \prod_{j=1}^{n} (t - \zeta_j)^{\alpha_j - 1} \dif t \,},
\end{equation}
and require
\begin{equation}
  \frac{L_k}{L_{n-1}}
  = \frac{\abs{w_{k+1} - w_k}}{\abs{w_0 - w_{n-1}}},
  \qquad k = 0, \dots, n - 2.
  \label{eq:param}
\end{equation}

\paragraph{Softmax reparameterisation.}
Given $p \in \RR^{n-3}$, define weights and cumulative pre-vertex positions:
\begin{equation}
  w_j(p) = \frac{e^{p_j}}{\displaystyle\sum_{i=1}^{n-3} e^{p_i} + 1},
  \qquad
  \zeta_{k+1} = \sum_{j=1}^{k} w_j(p).
  \label{eq:softmax}
\end{equation}
Since $e^{p_j} > 0$, each $w_j > 0$ and the cumulative sums are strictly
increasing. Let $S = \sum_i e^{p_i}$; then $\sum_j w_j = S/(S + 1) < 1$, so
every $\zeta_k \in (0, 1)$. The ordering and box constraints are thus
automatic, and the optimisation over $p \in \RR^{n-3}$ is unconstrained.

\paragraph{Solver and quadrature.}
System~\eqref{eq:param} is solved by Levenberg--Marquardt least squares via
SciPy's \texttt{least\_squares} routine. Each side-length integral is
evaluated by $N = 500$-node Gauss--Legendre quadrature on $[-1, 1]$ after
affine rescaling to $[\zeta_k, \zeta_{k+1}]$; nodes and weights are
pre-computed and reused across iterations, so each evaluation is a single
vectorised dot product. Levenberg--Marquardt update details are in
Appendix~\ref{app:lm}.

Figure~\ref{fig:prevertex-polygon} shows the recovered pre-vertex layout for
the 12-vertex Boulder County polygon alongside the polygon itself, with each
$\zeta_k \leftrightarrow w_k$ pair colour-matched. The concentration of
pre-vertices toward one side of the real axis directly visualises the
crowding phenomenon of Section~\ref{sec:crowding}: vertices on the narrow
western flank of the polygon share their pre-vertex region with several
close neighbours.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{figures/figC_prevertex_polygon.png}
  \caption{\textbf{Pre-vertex $\zeta_k \leftrightarrow$ polygon vertex $w_k$
  correspondence.} Matching colours identify each pair. Black squares in
  panel (a) mark the three M\"obius-fixed anchors
  $\zeta_0 = -1, \zeta_1 = 0, \zeta_{n-1} = 1$; the eight filled circles
  are free pre-vertices recovered by the solver. Panel (b) gives each
  vertex with its interior angle $\alpha_k \pi$. Clustering of pre-vertices
  in $[0, 0.5]$ reflects the asymmetric edge-length distribution of the
  simplified Boulder boundary.}
  \label{fig:prevertex-polygon}
\end{figure}

\paragraph{Determining $A$ and $C$.}
Once the pre-vertices are known, $A, C \in \CC$ are obtained from the
overdetermined linear system $A + C \cdot I_k = w_k$ for
$k = 0, 1, \dots, n - 1$, where $I_k$ is the SC integral from a reference
point $\zeta_{\mathrm{ref}} = 0.5i$ to $\zeta_k + \varepsilon$ (with a small
complex offset $\varepsilon$ to avoid the branch point). The least-squares
solution is computed via \texttt{numpy.linalg.lstsq}.

\subsection{Forward Map and Streamline Computation}
\label{sec:forward}

All flow results are produced via the \emph{forward-map} approach. A dense
$400 \times 400$ grid of $\zeta$ values in $\HH$ is mapped forward to
$z = f(\zeta)$, each SC integral evaluated by a $400$-node Gauss--Legendre
rule along an L-shaped path in $\HH$ that clears the real-axis branch cuts
(Figure~\ref{fig:branch-path}). The complex potential $W(\zeta)$ is evaluated
analytically at each $\zeta$ point. The resulting scattered pairs
$(z_i, W(\zeta_i))$ are interpolated onto a regular physical-plane grid
(\texttt{scipy.interpolate.griddata}, linear method), and contours of
$\Imag W$ (streamlines) and $\Real W$ (equipotentials) are extracted.

This avoids the per-pixel Newton iteration that inverse mapping would require.
Grid points from $\HH$ land strictly inside $\Omega$ by construction, so the
interpolated field is zero outside $\Omega$ with smooth contours at the
boundary.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.78\textwidth]{figures/figG_branch_cut_path.png}
  \caption{\textbf{L-shaped integration path used in the forward SC map.}
  Red markers on the real axis are the pre-vertices $\zeta_k$; short red
  segments below $\RR$ indicate the branch cuts of the integrand
  $\prod_k (t - \zeta_k)^{\alpha_k - 1}$. The blue square is the reference
  point $\zeta_{\mathrm{ref}} = 0.5i$; the green star is a target $\zeta$
  in $\HH$. The path is taken horizontally within $\HH$ to the target's
  real part (horizontal leg at $\Imag\,\zeta = 0.5$, passing above the
  pre-vertices on $\RR$), then vertically to the target. Both legs stay
  strictly above the real axis, so they never cross a branch cut (which
  extends downward from each $\zeta_k$ into the lower half-plane),
  preserving the chosen branch of the SC integrand.}
  \label{fig:branch-path}
\end{figure}

\subsection{Verification on the Rectangle Map}
\label{sec:verify}

Before applying the pipeline to Boulder, we verify it against the one
polygonal case with a closed-form SC map: the rectangle. The classical
construction~\citep[Ch.~5]{Nehari52} uses the symmetric pre-vertex set
$\{-1/k, -1, 1, 1/k\}$ with elliptic modulus $0 < k < 1$, giving
\begin{equation}
  f(\zeta) = \int_0^\zeta
    \frac{\dif t}{\sqrt{(1 - t^2)(1 - k^2 t^2)}}
  = F(\arcsin \zeta,\, k),
  \label{eq:rect-ellip}
\end{equation}
with $F$ the incomplete elliptic integral of the first
kind~\citep[{\S}19.2]{NIST10}. The corner images are
$f(\pm 1) = \pm K(k)$ and $f(\pm 1/k) = \pm K(k) + i K'(k)$, so the image
rectangle has width $2K(k)$ and height $K'(k)$; its conformal modulus
matches Section~\ref{sec:crowding}.

\paragraph{Adaptation to the project's parameterisation.}
The softmax reparameterisation~\eqref{eq:softmax} constrains all free
pre-vertices to $(0, 1)$ and fixes three anchors at $\{-1, 0, 1\}$. Values
$\pm 1/k$ with $k < 1$ lie outside this range, so the classical symmetric
placement cannot be used directly. The two normalisations are related by the
unique real M\"obius transformation matching the ordered cross-ratio on the
real axis:
\begin{equation}
  [-1, 0; s, 1] = [-1/k, -1; 1, 1/k],
\end{equation}
where $[a, b; c, d] = (a - c)(b - d) / [(a - d)(b - c)]$. Computing both
sides,
\begin{equation}
  \frac{1 + s}{2s} = \frac{(1 + k)^2}{4k}
  \qquad\Longrightarrow\qquad
  s(1 + k)^2 = 2k(1 + s)
  \qquad\Longrightarrow\qquad
  s = \frac{2k}{1 + k^2}.
  \label{eq:s-of-k}
\end{equation}
This gives a one-to-one correspondence between $k \in (0, 1)$ in the classical
picture and the single free pre-vertex $s \in (0, 1)$ in our pipeline. For a
target rectangle of conformal modulus $m(R) = 2K(k) / K'(k)$, the pipeline
must recover the corresponding $s$ determined by~\eqref{eq:s-of-k}.

\paragraph{Numerical verification.}
Take $m(R) = 2$ (the standard $2 \times 1$ rectangle), which corresponds to
$K(k) = K'(k)$, hence $k = 1/\sqrt{2}$ and
$s_{\text{exact}} = 2\sqrt{2}/3 \approx 0.94281$. The
softmax~+~Levenberg--Marquardt solver converges to
$s_{\text{recovered}} = 0.94296$, giving absolute error
$|s_{\text{recovered}} - s_{\text{exact}}| \approx 1.5 \times 10^{-4}$ on
the pre-vertex position and side-length residual $\|r\|_2 \approx 7 \times 10^{-5}$.

The residual floor is set by the plain Gauss--Legendre quadrature of the SC
integrand, which has integrable $(t - \zeta_k)^{-1/2}$ branch-point
singularities at each interval endpoint; $N = 500$ nodes without endpoint
substitution gives accuracy $O(N^{-1}) \approx 2 \times 10^{-3}$ on each
sub-integral, and this propagates into the pre-vertex recovery. A
Duffy-type substitution $t - \zeta_k = u^2$ would absorb the singularity and
push the convergence to near machine precision, at the cost of re-working the
quadrature module. The pipeline's current accuracy of $\sim 10^{-4}$ on the
pre-vertex is sufficient for the qualitative Boulder results of
Section~\ref{sec:apply}.
Figure~\ref{fig:verify} shows the target configuration and the LM
convergence history.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{figures/figA_rectangle_verification.png}
  \caption{\textbf{Rectangle verification of the SC pipeline.} Target:
  $m(R) = 2$ rectangle, exact $s = 2\sqrt{2}/3 \approx 0.94281$; recovered
  $s \approx 0.94296$. (a) Upper half-plane $\mathbb{H}$ with the pipeline's
  pre-vertex layout: blue squares are the three M\"obius-fixed anchors
  $\{-1, 0, 1\}$; the red circle is the free pre-vertex $s$ at its exact
  position for $k = 1/\sqrt{2}$. A rectangular reference grid on
  $\mathbb{H}$ is overlaid to illustrate the conformal-grid image structure
  used in Section~\ref{sec:apply}. (b) Levenberg--Marquardt convergence:
  the side-length residual cost $\tfrac{1}{2}\|r\|_2^2$ plateaus near
  $3 \times 10^{-9}$ while the pre-vertex error
  $|s_{\text{computed}} - s_{\text{exact}}|$ plateaus near
  $1.5 \times 10^{-4}$; the two curves stall at different floors,
  consistent with the $O(N^{-1})$ quadrature convergence rate at
  branch-point endpoints.}
  \label{fig:verify}
\end{figure}

\section{Application to the Boulder Polygon}
\label{sec:apply}

\subsection{Polygon Simplification}

Figure~\ref{fig:poly} shows the original 2752-vertex TIGER/Line boundary of
Boulder County alongside the 12-vertex polygon used in the SC computation.
Douglas--Peucker removes fine-scale boundary undulations; the interior-angle
filter then enforces $\alpha_k \in [0.35\pi, 1.75\pi]$. The aspect ratio and
dominant geometric features are faithfully preserved.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.80\textwidth]{figures/fig1_polygon_comparison.png}
  \caption{\textbf{Boulder boundary before and after SC preprocessing.}
  Left: original 2752-vertex TIGER/Line polygon~\citep{TIGER25}.
  Right: 12-vertex simplification after Douglas--Peucker reduction and
  iterative angle filtering with bounds $\alpha_k \in [0.35\pi, 1.75\pi]$.
  The simplified polygon is the domain $\Omega$ for all subsequent
  SC computations.}
  \label{fig:poly}
\end{figure}

\subsection{Uniform Ideal Flow}
\label{sec:res-uniform}

\paragraph{Topological note: closed streamlines are intrinsic.}
Uniform flow in $\HH$ has streamlines $\Imag \zeta = \eta_0$, which are
horizontal lines running from $\zeta \to -\infty$ to $\zeta \to +\infty$. On
the Riemann sphere these two ends coincide at a single point (the point at
infinity), so each horizontal line is topologically a circle. The SC map
sends that single point to one fixed vertex on $\partial \Omega$, and every
streamline therefore becomes a closed curve inside $\Omega$. The circulatory
appearance of Figure~\ref{fig:uniform} is a topological consequence of
conformally mapping $\HH$ onto a bounded polygon. To obtain crossing
streamlines that enter one side and exit another, the baseline potential
must be replaced with a source-sink pair
$W = U \log[(\zeta - \zeta_{\mathrm{src}})/(\zeta - \zeta_{\mathrm{sink}})]$,
whose streamlines run from source to sink.

\paragraph{Uniform flow results.}
Figure~\ref{fig:uniform} presents the uniform flow $W = U\zeta$ in $\Omega$.
Streamlines $\psi = \mathrm{const}$ in panel~(a) stay inside $\partial \Omega$,
satisfying the no-penetration condition exactly by construction. Panel~(b)
overlays streamlines and equipotentials; their orthogonality throughout the
interior is the definitive visual confirmation of conformality, a consequence
of the Cauchy--Riemann equations~\eqref{eq:CR} wherever $f'(\zeta) \neq 0$.

\begin{figure}[H]
  \centering
  \begin{subfigure}[b]{0.48\textwidth}
    \includegraphics[width=\textwidth]{figures/fig2_streamlines.png}
    \caption{Streamlines $\psi = \mathrm{const}$ (blue).}
  \end{subfigure}\hfill
  \begin{subfigure}[b]{0.48\textwidth}
    \includegraphics[width=\textwidth]{figures/fig4_combined.png}
    \caption{Conformal grid (streamlines + equipotentials).}
  \end{subfigure}
  \caption{\textbf{Uniform flow $W = U\zeta$ in the 12-vertex Boulder County
  polygon $\Omega$.} Streamlines are the forward images of horizontal lines
  $\Imag \zeta = \eta_0$ in $\HH$; equipotentials are images of
  $\Real \zeta = \xi_0$. The SC map $f : \HH \to \Omega$ preserves angles
  locally, so the two families meet orthogonally throughout the interior.
  Grid resolution $400 \times 400$ in $\HH$; 50 contours per family.
  Closed-streamline appearance is a topological consequence discussed above.}
  \label{fig:uniform}
\end{figure}

\paragraph{Geometric view of the SC map.}
Figure~\ref{fig:conformal-grid} shows the conformal grid from a purely
geometric angle: a rectangular mesh in $\HH$ (horizontal lines
$\Imag\,\zeta = \eta_0$ in blue and vertical lines $\Real\,\zeta = \xi_0$
in orange) pushed forward through $f$ into $\Omega$. Unlike
Figure~\ref{fig:uniform}, no flow interpretation is attached; the figure
shows only how $f$ deforms straight lines in $\HH$ into smooth curves that
meet at 90\textdegree{} throughout the polygon interior. This is the
signature geometric visualisation of a conformal map.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.68\textwidth]{figures/figD_conformal_grid.png}
  \caption{\textbf{Forward image of a rectangular grid in $\mathbb{H}$
  under the SC map $f$.} Blue curves are images of horizontal lines
  $\Imag\,\zeta = \eta_0$ for $\eta_0 \in [0.03, 1.8]$; orange curves are
  images of vertical lines $\Real\,\zeta = \xi_0$ for
  $\xi_0 \in [-2.4, 2.4]$. The two families remain orthogonal throughout
  $\Omega$, confirming conformality of $f$. Compare with
  Figure~\ref{fig:uniform}(b), which shows the same type of conformal grid
  labelled as flow streamlines and equipotentials.}
  \label{fig:conformal-grid}
\end{figure}

\subsection{Doubly-Connected Flow with an Urban Obstacle}
\label{sec:res-urban}

The downtown commercial core (OSMnx~\citep{BG17} landuse data, $0.82$\,km$^2$)
is mapped to $\HH$ via $f^{-1}$; its pre-image is enclosed by a minimum
enclosing circle (Welzl's algorithm~\citep{Welzl91}) of radius $a$ centred at
$\zeta_0 \in \HH$. The urban-obstacle potential~\eqref{eq:urban} then applies,
with achieved separation ratio $\Imag(\zeta_0)/a \approx 5.36$ for the
Boulder run, giving an $O\bigl((a/\Imag\,\zeta_0)^2\bigr) \approx 3.5\%$
leading-order approximation error.

Figure~\ref{fig:urban} shows the result. The Milne-Thomson dipole at
$\zeta_0$ enforces no-penetration on the enclosing circle; the image term
at $\conj{\zeta_0}$ restores $\psi = 0$ on $\RR$. Streamlines deflect
visibly around the purple obstacle, with local acceleration on its flanks.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.65\textwidth]{figures/fig7_urban_flow.png}
  \caption{\textbf{Doubly-connected flow with urban-core obstacle via the
  Milne-Thomson circle theorem~\eqref{eq:urban}.} Downtown Boulder commercial
  core shown in purple. Separation ratio $\Imag(\zeta_0)/a \approx 5.36$;
  order-of-magnitude approximation error $\sim 3.5\%$. Streamlines deflect
  around the obstacle, with a local acceleration on its flanks. Same grid
  resolution and contour counts as Figure~\ref{fig:uniform}.}
  \label{fig:urban}
\end{figure}

\paragraph{Velocity magnitude.}
Figure~\ref{fig:speed} shows $|W'(z)|$ for both flow models as heatmaps.
In the uniform case, the speed peaks at the polygon vertices (the SC map
has $f'(\zeta_k) = 0$ singularities, so
$|W'(z)| = U/|f'(\zeta)| \to \infty$ as $z \to w_k$), giving the expected
corner-acceleration signature. In the urban-obstacle case, the dipole term
additionally compresses the flow on the flanks of the downtown core,
producing two localised acceleration regions north and south of the
obstacle that the streamline figure only hints at qualitatively.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{figures/figF_velocity_heatmap.png}
  \caption{\textbf{Normalised velocity magnitude $|W'(z)|/U$ for the two
  flow models.} (a) Uniform flow: speed rises sharply near polygon vertices
  where $f'(\zeta_k) \to 0$. (b) Urban-obstacle potential: two additional
  bright regions on the flanks of the downtown core quantify the ``local
  acceleration'' visible in Figure~\ref{fig:urban}. Colour bar clipped to
  the 5th--80th percentile to suppress the finite-grid blow-up at vertex
  branch points while keeping the flank acceleration visible.}
  \label{fig:speed}
\end{figure}

\paragraph{Remark on modularity.}
Comparing Figures~\ref{fig:uniform}(a) and~\ref{fig:urban}, both panels
share the same SC map $f : \HH \to \Omega$ and the same normalised polygon,
so every visible difference comes from the analytic form of $W(\zeta)$ alone
(uniform flow vs.\ the urban-obstacle dipole~\eqref{eq:urban}). The SC map
is computed once; the physical model is switched by evaluating a different
closed-form expression in $\HH$.

\section{Conclusions}
\label{sec:conclusions}

The Schwarz--Christoffel transformation provides an exact, closed-form
analytic framework for ideal fluid flow in a simply connected polygonal
domain. The forward-map approach evaluates analytically tractable potentials
in $\HH$ and maps the results to $\Omega$, yielding streamlines that satisfy
the no-penetration condition exactly by construction. Two complex potentials
were derived in detail and visualised on the Boulder polygon: uniform flow
and a doubly-connected model with a Milne-Thomson interior obstacle. A
closed-form verification on the rectangle-to-half-plane map (an
elliptic-integral identity) confirmed the numerical pipeline to within the
expected quadrature-limited accuracy ($\sim 10^{-4}$).

The principal mathematical content is the interplay between conformal-mapping
theory (Riemann Mapping Theorem, SC integral formula, interior-angle
structure, M\"obius normalisation, Schwarz reflection) and ideal-flow theory
(complex potential, method of images, Milne-Thomson circle theorem). The
SC formula~\eqref{eq:SC} encodes polygon geometry through the exponents
$\alpha_k - 1$: each vertex angle becomes a branch-point strength. The
Cauchy--Riemann equations, through the conformality of $f$, guarantee that
every solution of Laplace's equation in $\HH$ pulls back to a solution in
$\Omega$.

A practical obstruction identified in Section~\ref{sec:crowding} is
\emph{SC crowding}: the pre-vertex spacing across a long region of aspect
ratio $L$ contracts exponentially in $L$, a rigid conformal invariant tied
to the modulus of the region. This forces the 2752-vertex Boulder County boundary
to be simplified to 12 vertices before any SC computation is feasible.

\paragraph{Future directions.}
Two extensions are natural.

\begin{itemize}[leftmargin=2em]
\item \textbf{Exact doubly-connected SC map.} The Milne-Thomson
  approximation~\eqref{eq:urban} introduces a small but non-zero
  approximation error (see Section~\ref{sec:res-urban}). An exact
  doubly-connected SC map~\citep[Ch.~5]{DT09}, implemented via the
  Schottky double or a circular slit canonical region, would eliminate
  this approximation and give a rigorous treatment.

\item \textbf{Multiply connected domains.} The formalism extends to domains
  with several obstacles via the Schottky-double conformal map, enabling an
  SC-style treatment of full building footprints rather than a single
  enclosing circle.
\end{itemize}

\bibliography{refs}

\clearpage
\appendix
\section{Levenberg--Marquardt Solver Details}
\label{app:lm}

Given the residual vector $r(p) \in \RR^{n-1}$ of~\eqref{eq:param} and
Jacobian $J = \partial r / \partial p$, Levenberg--Marquardt updates
\begin{equation}
  p^{(t+1)} = p^{(t)}
    - \bigl( J^\top J + \lambda_t\, \mathrm{diag}(J^\top J) \bigr)^{-1}
      J^\top r(p^{(t)}),
  \label{eq:lm}
\end{equation}
where $\lambda_t$ is an adaptive damping parameter. When $\lambda_t \to 0$
the update reduces to the Gauss--Newton step (fast second-order convergence
near a good solution); when $\lambda_t \to \infty$ it reduces to a small
gradient-descent step (stable near ill-conditioned regions). The damping is
increased whenever a proposed step fails to reduce $\|r\|_2$ and decreased
on successful steps. For the Boulder County $12$-vertex problem the solver reduces the
LM cost to $\approx 1.1 \times 10^{-2}$ after five restart passes,
reflecting mild crowding of six pre-vertices into the interval
$[0.688, 0.700]$ on the real axis.

\section{Code and Reproducibility}
\label{app:code}

All source code is available at the project GitHub
repository\footnote{\url{https://github.com/McDonaldCrispyThigh/Ideal-Flow-SC}}.
The two-model pipeline is reproduced by:
\begin{verbatim}
  pip install -r requirements.txt
  python main.py --shapefile data/raw/tl_2025_08_county --urban --grid 80
\end{verbatim}
Key modules: \texttt{src/polygon.py} (boundary preprocessing),
\texttt{src/angles.py} (interior-angle computation),
\texttt{src/sc\_solver.py} (SC parameter problem, Gauss--Legendre quadrature,
forward map),
\texttt{src/urban.py} (OSM obstacle, Welzl minimum enclosing circle),
\texttt{src/sc\_solver\_dc.py} (doubly-connected potentials), and
\texttt{src/visualization.py} (figure generation).

\noindent\textbf{Software:} Python~3.14, NumPy~2.2, SciPy~1.15,
Shapely~2.0, GeoPandas~1.0, OSMnx~2.0~\citep{BG17}, Matplotlib~3.10.

\end{document}
